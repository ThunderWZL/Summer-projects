# 老师项目要求与建议提炼

> 说明：以下判断只提炼四天逐字稿中与课程项目有关的内容；“对 CampusParkGuard 的启发”是将老师原意映射到当前项目，不扩写完整方案。逐字稿转写中将 RAG、Dify 分别误写为“IG”“Defi”等，本文统一使用标准名称。

## 1. 老师明确提到的项目/作业路线

| 路线 | 老师原意概括 | 对 CampusParkGuard 的启发 | 是否适合作为主线 |
|---|---|---|---|
| 纯视觉算法路线 | 第一、二天的深度学习与 YOLO 适合做纯算法作业：自行收集和标注数据，训练目标检测模型，能力足够时还可修改模型；这是往年课程作业的常见路线。（第四天，第 284—285 行） | YOLO 检测应是项目的可靠技术底座；可用校园场景数据完成训练、验证、指标展示。考虑整个项目还要覆盖 RAG 与 Agent，YOLO 微调宜作为最后的独立增强模块，而不是阻塞前面业务闭环。 | **适合作为核心模块，但不必单独承担整条主线。** |
| 大模型本地部署路线 | 仅下载已有权重并用现成工具部署，可发挥空间小、自己写的代码少；若选择这条路线，需要叠加低算力设备、模型移植和简单界面，才能体现技术难度。（第四天，第 286—287 行） | CampusParkGuard 不应把“本地跑通一个大模型”当作主要成果。除非项目明确加入边缘设备部署，否则直接使用 API 或适当的小模型更能把时间留给检测、规则、检索和报告闭环。 | **不适合单独作为主线。** |
| Python 大模型应用 / RAG 路线 | 可基于自己的数据构造 RAG 小软件，但不能直接照搬课堂基础代码，必须继续丰富功能；LangChain 需要编码，能掌握更多细节和获得更高灵活性。（第四天，第 224—226、275—281 行） | 校园管理条款适合作为私有、可更新知识库；要把检索结果真正接入违规解释、依据引用和报告生成，而不是只做通用聊天机器人。 | **适合作为应用主线的重要一段。** |
| Agent / 可视化工作流应用路线 | 可用低代码工作流搭建更复杂的应用，部署后嵌入前端；关键是先拆解任务和处理逻辑，再用节点组合实现。Dify 更适合快速原型与交付，LangChain 更适合需要代码和底层控制的开发者。（第四天，第 275—288、555 行） | 将“检测结果 → 区域规则 → 条款检索 → 问题卡片/报告”拆成明确步骤；Agent 负责组织工具和生成成果，不应代替确定性的检测与规则判断。 | **适合作为串联主线，但必须有清晰边界。** |

## 2. 老师对一个月课程设计的隐含要求

- **工作量要与周期、组员数匹配。** 作业为两人一组、约在 8 月底提交；老师给出的三条路线都是可在现成框架上继续完成的路线，而不是从零重造基础模型或平台。（第四天，第 284—290 行）
- **必须形成可运行、可演示的成果。** 老师多次要求把工具实际安装和部署起来、真正操作并做出应用；应用还可嵌入前端。CampusParkGuard 因此至少要能完整演示一次从校园场景图片输入到问题卡片和报告输出的流程，视频只宜留作扩展。（第四天，第 288、555 行）
- **不能把课堂 demo 原样当成作业。** RAG 基础代码只展示基本能力，作业必须自行丰富功能；第一天的分类 demo 也明确留下了更复杂数据集、模型和完整验证供学生扩展。（第一天，第 727—739 行；第四天，第 224—226 行）
- **技术覆盖应服务同一任务，而不是堆名词。** 老师将算法、部署、应用分为不同路线，同时强调工作流应先做任务和逻辑拆解。CampusParkGuard 可以跨路线组合，但每个模块都必须对校园巡查闭环有明确输入和输出。（第四天，第 284—290、555 行）
- **代码和可控细节要足以体现个人工作。** 单纯使用现成部署工具“没有多少代码”；LangChain 源码方式、业务规则、前后端集成和结果结构化更能体现编码工作。Dify 可用于快速验证，但若完全依靠拖拽，需要用自定义逻辑、插件或外部程序补足项目贡献。（第二天，第 611—633 行；第四天，第 275—288 行）
- **视觉部分的主要工作应落在自己的数据上。** 老师指出 YOLO 项目的重点是准备任务数据、保证标注格式正确，并完成训练/验证/测试；官方脚本已经提供训练与推理能力。（第二天，第 567—579、611—637 行）
- **应有可量化和可追溯的展示材料。** YOLO 训练可输出损失、Precision、Recall、mAP、PR 曲线、CSV 和最好/最后权重；RAG 应先测试分块与召回结果，正式输出应带条款来源，以提高可靠性。（第二天，第 635—639 行；第四天，第 20—24、533—539 行）
- **扩展课堂 demo 的方向应是业务化。** 老师建议增加界面、数据库/模型选择、结构化输出、前端嵌入和更完整逻辑，而非仅增加一个模型调用。对 CampusParkGuard，扩展应集中在区域配置、规则证据、问题卡片和报告导出等可见功能。（第四天，第 220—226、479—485、288 行）

## 3. CampusParkGuard 应该遵循的设计原则

1. **先做可演示闭环，再做算法增强。** 现成 YOLO 模型和官方推理脚本足以快速形成基线；使用预训练权重微调比从头训练更快、所需轮次更少。因此先贯通检测、规则、RAG、Agent 和报告，最后把 YOLO 微调作为可独立验收的增强项。（第二天，第 507—511、577—579、611—639 行）
2. **YOLO 选择工业界成熟实现，不把重写检测框架当贡献。** 老师明确指出 Ultralytics 的项目更稳定、更工业级，实际工业项目通常采用其代码；课程阶段掌握和使用现成模型已足够。（第二天，第 203—209、631—635 行）
3. **视觉效果主要靠场景数据和规范评估证明。** 自行准备校园场景数据，保证图片、标签、类别、训练/验证/测试划分一致，并同时保留定性检测图和 mAP、Precision、Recall、损失等定量结果。（第二天，第 567—579、611—639 行）
4. **区域规则必须由确定性程序执行。** 老师指出传统软件逻辑稳定、输入输出可预测，而 Agent 虽能自主规划复杂任务，却不如传统软件稳定。禁停区、越界、占道等几何规则应由代码判断，不能让大模型凭描述猜结论。（第四天，第 264 行）
5. **条款知识优先用 RAG，不用大模型微调硬记。** RAG 更适合私有、可更新资料，门槛较低，还能提供引用、缓解幻觉；大模型微调需要至少数千条高质量数据，准备耗时且不适合频繁更新的知识。（第四天，第 10—24 行）
6. **RAG 必须验证“检索到了什么”。** 检查分块是否合理，测试关键词/向量/混合检索及 Top-K、阈值，先验证召回再接大模型；报告中的处理依据应保留来源，而不是只输出无出处的自然语言结论。（第四天，第 521—543 行）
7. **Agent 只负责拆解、调用与生成成果。** 按“检测事实—规则结论—条款证据—问题卡片/报告”的顺序组织工具；为检索为空、输入缺失等情况设置显式分支和稳定输出格式。老师强调工作流要先拆任务、拆逻辑，并可用分类、条件、检索和结构化输出节点组合实现。（第四天，第 479—493、539—555 行）
8. **作业成果必须明显超出课堂 demo。** 增加校园业务界面、可配置区域、结构化问题卡片、报告导出和完整异常处理；可用 Dify 快速验证流程，但最终实现要保留足够的自写代码和可解释逻辑。（第四天，第 224—226、275—288、555 行）

## 4. 不适合作为主线的方向

- **从零训练或完整部署大语言模型。** 顶级大模型训练周期以半年计，并非一个月能完成；完整模型对存储、显存和算力要求极高。课程项目没有必要在这条线上消耗主体时间。（第三天，第 56—70 行）
- **只下载权重并展示本地对话。** 老师明确判断其可发挥空间和代码量都很小；除非额外完成低算力设备移植、量化、服务化和界面，否则不足以支撑完整课设。（第三天，第 609—649 行；第四天，第 286—287 行）
- **复杂的大模型微调或用微调维护校园条款。** 大模型微调需要数千条高质量数据、较高算法能力和超参数经验，且无法经济地追随频繁更新的知识；CampusParkGuard 的条款依据应交给 RAG。（第四天，第 10—20 行）
- **把修改 YOLO 网络结构设为必须完成的主线。** 老师允许有能力时修改模型，但也指出多数情况下官方模型配置已够用，课程当前目标是先会使用现成模型。结构创新可作为额外研究项，不能阻塞可运行系统。（第二天，第 577—579、631—635 行；第四天，第 284—285 行）
- **实时视频流巡检平台。** YOLO 官方脚本虽然支持视频、摄像头和网络流，但老师的 CPU 演示中较大模型单帧耗时超过 100 ms，达不到实时处理。CampusParkGuard 应先保证图片巡检稳定可演示，视频抽帧可作为后续扩展，不把实时平台作为课程主线。（第二天，第 521、555 行）
- **完全依赖低代码拖拽、没有自写业务逻辑。** Dify 适合快速原型和交付，但底层自定义能力弱；若只复刻课堂工作流，会同时缺少代码贡献和项目辨识度。可把它用作原型或编排层，核心规则、数据处理和结果产出仍应有自己的实现。（第四天，第 275—288、555 行）
- **一开始追求完整企业级、多系统、多 Agent 工作流。** 老师举出的跨系统审批、多源数据整合等属于 Agent 的复杂企业场景，同时也提醒 Agent 稳定性不如传统软件。一个月课设应只实现 CampusParkGuard 所需的有限工具链和明确失败分支，不扩展成通用企业平台。（第四天，第 264—280 行）
