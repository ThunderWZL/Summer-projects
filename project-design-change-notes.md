# CampusParkGuard 项目设计变更说明

生成日期：2026-07-13

本次修改综合了以下四份专项分析，而不是直接拼接其中内容：

- `teacher-project-guidance.md`：四天逐字稿中的作业路线、一个月课设要求、设计原则和风险提醒；
- `course-tech-coverage-map.md`：四天课程技术点的 MVP / 增强 / 不采用分层；
- `course-code-reuse-map.md`：课堂 YOLO、Qwen、LangChain 和 PyTorch 代码的复用价值与缺口；
- `external-reference-map.md`：官方文档、学校/政府规定及少量开源案例对方案可行性和边界的支撑。

冲突取舍统一采用三个优先级：**一个月可完成、课程技术相关、能够稳定演示**。

## 1. 修改的章节

| 章节 | 主要修改 |
|---|---|
| 1. 项目概述 | 明确项目是两人、约一个月的课程设计，不是执法、实时监控或企业工单平台；重写输入、输出、技术主线，并新增老师建议的落地表 |
| 2. 设计原则 | 强化事实来源、依据与事实分离、受控 Agent、失败降级、课堂 Demo 业务化扩展和隐私边界 |
| 3. 总体架构 | 增加检测事实、疑似问题事实、条款证据、Schema 校验和模板降级层；把微调改为虚线接入的独立增强 |
| 4. 核心流程 | 修正为“YOLO → 规则事实 → RAG → 受控 Agent → 卡片 → 报告”，补充无条款和 LLM 失败分支，简化为单机后台线程 |
| 5. 模块设计 | 重写 YOLO、微调、区域、规则、RAG、Agent、卡片和报告模块；区分课堂 YOLOv5 与工程版本；新增受状态门控的 LLM Function Calling 和独立生成模块 |
| 6. 数据关系 | 用 `POLICY_QUERY` 记录检索与未匹配状态、`POLICY_MATCH` 只记录真实条款命中；移除整改闭环；增加事实来源和数据权限矩阵 |
| 7. 推荐技术栈 | 为 MVP 每层固定一条主路线；新增课堂代码复用边界，明确区域规则和真正工具 Agent 需自行实现 |
| 8. 范围边界 | 改为 MVP、最后独立增强、其他增强、不采用四级边界，并补充本地存储与公开演示隐私要求 |
| 9. MVP | 给出九步闭环、同步 Mermaid 图、完整性判定和微调解耦条件；PDF、整改对比和视频抽帧退出 MVP |
| 10. 课程技术点对应关系 | 新增覆盖四天课程的映射表，明确每个技术点的层级和答辩证据 |
| 11. 一个月实施计划 | 新增四周增量计划、两人分工和“先冻结 MVP、最后限时微调”的实施顺序 |
| 12. 测试与验收 | 新增模块测试、固定端到端验收集、事实与引用完整性不变量及降级测试 |
| 13. 可扩展方向 | 只保留与当前主线直接相关的八项轻量增强，明确不得同时展开 |
| 14. 答辩亮点 | 从“功能数量”调整为事实边界、证据追溯、课程映射、可控 Agent、微调取舍与可复现性 |
| 15. 外部依据与适用限制 | 新增官方、学校和政府来源链接，并声明校规时效、示例条款和非执法用途限制 |

## 2. 加入的老师要求和课程依据

- 采用“先跑通可演示闭环，再做算法增强”的顺序，回应老师对可运行、可展示和周期匹配的要求。
- 不把课堂 Demo 原样作为作业：课堂代码提供推理、训练和 RAG 骨架，项目自行增加区域配置、确定性规则、证据 Schema、问题卡片、工具轨迹与报告整合。
- 视觉部分先使用成熟 Ultralytics 实现；自己的训练工作集中在小规模数据、标注、验证、指标和误差分析。
- 不把“下载权重并本地聊天”作为项目主线；LLM 只是结构化生成组件，主要代码贡献仍在业务逻辑与集成。
- 采用代码版 LangChain 作为主 RAG 实现；Dify 只用于可选工作流复刻，避免零代码拖拽成为唯一成果。
- 根据老师对传统程序稳定性与 Agent 不确定性的提醒，将空间规则放入确定性代码；Agent 只在当前状态允许的集合内由 LLM 发起 Function Calling，控制器校验参数与轮次。
- 训练曲线、Precision、Recall、mAP、检索结果、引用来源和 Agent trace 被纳入报告与答辩证据。

## 3. 新增的课程技术对应关系

新章节将课程内容分为以下层次：

- 第 1 天：CNN、深度学习和 PyTorch 推理进入 MVP；Dataset/DataLoader、损失、反向传播和优化器随 YOLO 微调作为增强。
- 第 2 天：YOLO 目标检测、图片推理、检测框、置信度、IoU/NMS 进入 MVP；明确课堂实战是 YOLOv5，外部工程版本不冒充课堂代码；自定义数据微调最后实施。
- 第 3 天：Qwen/Hugging Face/ModelScope/API 路线中选择一种，完成受约束 LLM 推理、Prompt 和结构化输出；不同时维护多后端。
- 第 4 天：RAG 的加载、切分、Embedding、Chroma、检索和生成，以及 LangChain/LCEL 和受控 LLM Function Calling Agent 进入 MVP；Dify 工作流仅作增强。
- 从零 CNN、LLM 微调、MoE、多 Agent、长期记忆和开放式联网明确不采用。

## 4. 确定为 MVP 的功能

MVP 是一条不依赖 YOLO 微调的完整链：

1. 上传单张校园场景图片并配置区域；
2. 冻结的预训练 YOLO 检测 person、bicycle、car、motorcycle；
3. 保存检测事实与证据图；
4. 3～5 个确定性区域规则生成疑似问题事实；
5. 20～40 条轻量条款库通过 LangChain + Chroma 检索依据；
6. 单个受控 Agent 由 LLM 在状态门控下调用五个白名单工具，控制器拒绝越权并限制轮次；
7. 选定一种 Qwen/兼容后端生成受约束字段，失败时模板降级；
8. 生成结构化问题卡片并提供 `待复核 / 确认 / 排除` 三态复核；
9. 汇总并导出 Markdown 报告，保存 Agent 调用轨迹。

## 5. 保持为增强或扩展的功能

- **最后独立增强**：YOLO 小样本微调、验证和基线比较；训练失败、未提升或仅完成实验设计都不影响 MVP。
- **可选增强**：Dify 最小工作流复刻、第二种 LLM 后端、PDF 导出、整改前后图片对比、车位占用统计、固定间隔视频抽帧、脱敏车牌识别。
- **明确排除**：实时多路视频、跨摄像头跟踪、3D、人脸识别、身份追踪、开放式多 Agent、企业工单、审批派单、多用户权限、大模型训练平台和生产级部署。

## 6. 为什么适合一个月课程设计

- 输入固定为单图，视觉类别来自 COCO 预训练模型，避免在数据和实时流上失控。
- 区域由人工配置，规则数量控制在 3～5 个，结论可复现、可单元测试。
- 条款库只有 20～40 条，本地 Chroma、SQLite 和文件系统足够，不需要外部基础设施。
- 前端固定为 Streamlit，后端是 FastAPI 单体和后台线程，不维护第二套前端或分布式队列。
- 默认使用课堂可运行的小参数本地 Qwen 路线，兼容 API 只是替换方案；模板降级避免演示被一次模型调用阻断。
- 四周计划在第三周打通闭环，第四周前半冻结和回归，后半才限时尝试微调。
- 每项课程技术都有可展示产物，同时删去与主线无关的模型训练和企业管理功能。

## 7. Mermaid、模块关系和数据结构同步调整

- 总体架构图新增 `DETECTION_FACT`、`RISK_FACT`、条款证据、受控 Agent、Schema 校验和模板降级，YOLO 微调以虚线可选权重接入。
- 时序图将风险事实固定在 RAG/Agent 之前，并加入 RAG 未匹配与 LLM 失败分支。
- 状态图新增 `organizing_evidence` 和 `validating_cards`，移除含义模糊的卡片自校验步骤。
- Agent 图由开放式 Planner 改为“LLM Function Calling + 五个白名单工具 + 状态门控 + 轮次上限”的受控流程，区别于固定 Workflow。
- ER 图增加 `POLICY_QUERY` 与 `POLICY_MATCH`：前者记录每次查询及 `unmatched`，后者必须关联真实条款；卡片与风险事实为一对零或一关系；核心关系中移除整改闭环。
- MVP 图修正为“事实 → 条款 → Agent → 卡片 → 报告”的顺序，不再先生成卡片再检索条款。
- 数据结构统一使用 `detection_id`、`rule_id`、`risk_fact_id`、`region_id`、`policy_status`、`review_priority` 和 `generation_mode`；删除含义不清的综合风险置信度，并注明复核优先级只是课程演示内部排序。

## 8. 使用的外部参考

- Ultralytics Predict、Train 和 COCO 官方文档：支撑单图结果字段、证据图、常见类别和独立微调流程。
- LangChain 官方 RAG 教程：支撑加载、切分、Embedding、向量库、检索、生成两阶段链路。
- Dify Knowledge、Workflow 和 Agent Strategy：只用于知识库测试、条件节点、参数 Schema 和最大迭代思想。
- Qwen 官方 Quickstart、阿里云 Qwen API 与 Hugging Face Pipeline：支撑本地/API 互换的统一推理适配层。
- 上海交通大学、海南大学、云南师范大学的校园管理规定，以及应急管理部消防/电动车资料：支撑区域类型和条款元数据设计。
- Ultralytics Parking Management、少量开源停车项目与 AI City Challenge：只参考 JSON 区域、证据可视化、基线/训练分离和任务拆分，不引入实时视频、多视角或赛事级方案。

限制：校规存在学校、校区和时间差异，正式演示前仍需复核目标学校有效原文；外部开源案例只证明组织方式可参考，不代表其模型、数据或工程质量已经在本项目验证。

## 9. 最终检查记录

检查日期：2026-07-13。

- **Markdown**：`markdown-it` 已成功解析 `project-design.md` 与本文件；表格管道数检查无不一致；24 个代码围栏成对闭合。
- **Mermaid**：确认共有 6 幅图，分别为总体架构、核心时序、任务状态、Agent、ER 和 MVP；时序图的 6 个 `loop/alt` 均有对应 `end`；逐图核对节点、先后关系、6/2 次工具预算、微调虚线依赖和数据实体与正文一致。
- **JSON 与 Schema**：6 个 JSON 示例均可解析；基线与微调检测示例键集合完全相同；全文无遗留 `risk_id`、`model_source` 或 `severity` 字段。
- **MVP 与微调**：MVP 从单图到 Markdown 报告可以只使用预训练基线；微调只在第四周后半、MVP 冻结后限时实施，失败、未提升或只交付实验设计均不影响闭环。
- **事实来源**：检测事实来自 YOLO，疑似问题事实来自 YOLO + 区域规则；“停放区外”和“同通道近距离共现”均只陈述单图可观察的空间关系，不推断停放、轨迹或混行行为；`review_priority` 明确不是官方风险等级。
- **RAG 权限**：RAG 只检索条款；`POLICY_QUERY` 可记录 `unmatched`，`POLICY_MATCH` 只关联真实条款；无命中时不补造编号、来源或处罚。
- **Agent**：Agent 是状态门控的真实 LLM Function Calling，不是普通 LCEL 链；每卡 6 次、每报告 2 次工具预算在文字、时序图和 Agent 图中一致；越权、缺事实、无条款、解析失败和引用失败均有分支。
- **范围禁项**：MVP 未包含企业工单、审批派单、多用户权限、复杂多 Agent、实时视频、人脸识别、身份追踪或未脱敏车牌。
- **课程映射**：专项 Agent 复核确认 PyTorch、YOLO、Qwen/本地或 API、RAG、LangChain、Dify 和 Agent 的课程归属及 MVP/增强分层一致；YOLOv5 课堂实战与 YOLOv8n 外部工程选项已明确区分。
- **外部依据**：专项 Agent 复核确认官方能力没有被写过头，学校/政府规定只用于条款结构和示例，校规时效与校区差异已有声明。
- **文件完整性**：主方案、变更说明和四份中间产物均存在且非空。

仍存在的验证限制：当前环境没有 Mermaid CLI，因此完成了代码块结构与逐图语义检查，但未实际渲染为 SVG/PNG；当前目录未被 Git 识别为仓库，因此没有 Git diff；本任务修改的是设计文档，尚无应用代码可执行端到端测试。正式演示前仍需按目标学校和当时有效规定复核条款链接与原文。
