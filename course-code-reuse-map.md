# 课堂代码可用性判断

## 1. 代码资产映射表

| 代码/目录 | 课堂功能 | 可服务项目模块 | 复用等级：高/中/低 | 复用方式 |
|---|---|---|---|---|
| `第1天-深度学习基础代码/1-数据读取/` | 用 `Dataset`、`DataLoader`、`ImageFolder`、自定义清单和 `transforms` 完成图片读取、标签解析、批处理与数据增强 | 2. YOLO 微调增强 | 中 | 借鉴“图片与标签组织—训练/验证划分—批量读取—增强”的流程意识；现有代码是二分类格式，不能直接读取 YOLO 检测框标签。 |
| `第1天-深度学习基础代码/2-不同搭建模型的方式/` | 展示 LeNet 的多种 PyTorch 搭建方式、前向传播、参数查看和 `DataParallel` | 1. YOLO 视觉检测；2. YOLO 微调增强 | 低 | 用于解释 YOLO 同样建立在卷积、特征图、`nn.Module` 和 PyTorch 训练机制上；项目不自行重搭 YOLO 网络，也不需要多卡训练。 |
| `第1天-深度学习基础代码/classify_demo/` | 给出分类数据集、CNN、损失、优化器、学习率调度、TensorBoard、模型保存与单图推理的基本闭环 | 2. YOLO 微调增强 | 中 | 复用实验组织方式和训练/推理分离思路；YOLO 的检测数据格式、损失、指标及官方训练入口仍以 Ultralytics 为准。 |
| `单层感知器模型.py`、`多层感知器异或问题求解.py` | 演示线性分类、前向计算、误差反传和参数更新的基础原理 | 2. YOLO 微调增强（原理说明） | 低 | 仅作为深度学习基础背景，不进入 CampusParkGuard 的实现链路。 |
| 第二天 YOLO 课件与逐字稿（未提供独立代码包） | 现场演示 YOLOv5 官方 `detect.py`/`train.py`、Ultralytics 包与 CLI、预训练权重推理、YOLO 数据集组织、训练日志和 mAP/PR 曲线 | 1. YOLO 视觉检测；2. YOLO 微调增强 | 高 | MVP 采用预训练模型做图片推理并读取类别、边界框和置信度；最后再按“图片/标签—数据 YAML—训练—验证—指标比较”实施独立微调实验。 |
| `第3天-大模型基础代码/hf-pipeline.py` | 用 HuggingFace `pipeline` 在 CPU 上加载 Qwen3-0.6B，并按 chat 消息生成文本 | 6. 问题卡片生成；7. 巡检报告生成；8. LLM 推理 | 高 | 可直接借鉴最小本地推理入口，封装为统一 LLM 调用适配层；输入必须是已确定的问题事实和检索条款，不能让模型自行判定风险。 |
| `第3天-大模型基础代码/hf-qwen.py` | 用 `AutoTokenizer`、`AutoModelForCausalLM`、chat template 和 `generate` 完成 Qwen 本地推理，并区分 thinking/content | 6. 问题卡片生成；7. 巡检报告生成；8. LLM 推理 | 中 | 适合需要控制模板、生成参数和输出解析时借鉴；须替换硬编码本机路径、限制输出长度，并避免依赖固定 token id 解析业务结果。 |
| `第3天-大模型基础代码/modelscope-qwen.py` | 通过 ModelScope 加载 Qwen 并执行本地生成 | 6. 问题卡片生成；7. 巡检报告生成；8. LLM 推理 | 中 | 作为 HuggingFace 之外的模型获取/加载备选；项目只保留一种实际运行路径，避免同时维护多套模型后端。 |
| `第3天-大模型基础代码/token_demo.py` | 演示分词、定长填充、token 查看与解码 | 8. LLM 推理 | 低 | 仅用于理解上下文长度与输入裁剪，不构成业务模块。 |
| `langchain代码.zip/1_大模型调用.py` | 使用 `ChatPromptTemplate + LLM + StrOutputParser` 组成 LCEL 文本生成链 | 6. 问题卡片生成；7. 巡检报告生成；8. LLM 推理 | 中 | 借鉴提示模板、模型与解析器解耦的组合方式；需改为结构化模板并注入检测事实、规则结果和条款引用，不照搬开放式聊天循环。 |
| `langchain代码.zip/2_不同格式数据加载与分割.py` | 加载 txt/PDF/CSV/HTML/目录文档，并用 `RecursiveCharacterTextSplitter` 分块 | 4. RAG 条款检索 | 高 | 条款库只选一种轻量、可维护的文本格式，保留条款编号和来源元数据后再切分；无需把课堂展示的全部加载器都带入项目。 |
| `langchain代码.zip/3_数据加载分割与嵌入.py` | PDF 加载与切分、本地 HuggingFace Embedding、Chroma 向量库持久化 | 4. RAG 条款检索 | 高 | 可借鉴“离线建库”流程，将 20～40 条校园管理条款嵌入并持久化；压缩包未附实际 embedding 模型和源文档，需由项目自行准备。 |
| `langchain代码.zip/4_检索.py` | 加载持久化 Chroma，创建 retriever，并用“检索上下文—Prompt—LLM—Parser”组成 RAG 链 | 4. RAG 条款检索；6. 问题卡片生成；7. 巡检报告生成；8. LLM 推理 | 高 | 复用检索链主干，但检索层应返回条款文本、编号和来源；生成层只整理引用，不得据此创造新的风险事实。 |
| `langchain代码.zip/5_DeepAnswerer.py` | 用 PyQt5 实现目录选择、问题输入、检索按钮和结果展示，并串接 Chroma RAG | 4. RAG 条款检索；6. 问题卡片生成；7. 巡检报告生成 | 中 | 可借鉴“选择资料/输入—触发处理—展示结果”的轻量交互节奏；不要求直接复用 PyQt 技术栈，也不扩展成复杂桌面系统。 |
| 未发现对应课堂代码：区域规则与 Agent 工具调用 | 已分配代码中没有区域几何判断、工具注册、白名单调用或调用轨迹实现 | 3. 区域规则判断；5. Agent 受控工具调用 | 低 | 不能把普通 LCEL RAG 链宣称为 Agent。区域规则需保持独立、确定性；Agent 只在项目内封装少量固定工具并记录输入输出。 |

## 2. 可直接借鉴的实现模式

1. **PyTorch 实验闭环**：借鉴 `classify_demo` 的数据划分、训练循环、损失记录、TensorBoard、模型保存和独立推理思路，用于组织 YOLO 微调实验；验证与检测指标应补齐并交由 YOLO 官方流程计算。
2. **YOLO 预训练推理**：沿用课堂演示的 Ultralytics/官方 `detect.py` 思路，对上传图片执行推理，提取类别、置信度和边界框，并保存标注图，直接支撑 MVP 的视觉证据。
3. **YOLO 独立微调**：沿用课堂的 `images/labels` 划分、YOLO 归一化标签、数据 YAML、预训练权重微调、训练日志及 mAP/PR 曲线比较；该流程最后实施且不改变 MVP 接口。
4. **HuggingFace pipeline 调用 Qwen**：`hf-pipeline.py` 是本地小模型推理的最短路径，适合先验证问题卡片和报告生成；将固定示例消息替换为受约束的结构化证据输入。
5. **Qwen/ModelScope 直接加载**：需要更细控制时，可在 `hf-qwen.py` 与 `modelscope-qwen.py` 两种模式中择一，统一 chat template、生成长度和输出解析，不同时维护两套实现。
6. **文档加载与递归切分**：借鉴 LangChain 加载器与 `RecursiveCharacterTextSplitter`，但只处理轻量条款库，并让每个分块携带条款编号、标题和来源。
7. **Embedding + Chroma 持久化检索**：借鉴本地 Embedding、离线建库、持久化目录和 retriever 的组合，使条款检索可重复运行且不依赖每次重新建库。
8. **Prompt + LLM + Parser 链**：借鉴 LCEL 管道把检索上下文、确定性风险事实和输出模板组合起来；简单 GUI 的“输入—触发—结果展示”可复用交互思路，但生成结果仍需结构校验。

## 3. 不建议直接复用的内容

- **感知器、异或 MLP 和 LeNet 示例**：它们用于讲清神经网络原理或分类网络搭建，不具备目标检测、边界框输出或校园规则能力；直接接入只会制造无关代码量。
- **`classify_demo` 的模型与训练脚本原样复制**：其任务是表情二分类，虽创建了验证集却未执行验证循环，且保存/加载整个模型对象；不能替代 YOLO 的检测训练、验证和权重管理。
- **分类数据清单原样复用**：现有清单只有“图片路径 + 类别”，而 YOLO 需要每个目标的 `class x_center y_center width height`；其中 `all.txt` 还含重复追加记录，不应作为项目数据模板。
- **`DataParallel` 示例**：一个月课程设计的小规模微调没有多卡需求，引入多卡兼容会增加环境和调试成本。
- **大模型示例中的固定运行参数**：本机绝对路径、CPU 强制设置、`max_new_tokens=32768`、固定 thinking 结束 token 等均是课堂演示条件，直接照搬会造成路径失败、推理过慢或解析脆弱。
- **同时接入 HuggingFace、ModelScope 和多个远程 API**：三者只是替代性调用路线；MVP 应选一种稳定路径，另一种最多写成可替换后端。
- **开放式聊天循环和高随机性生成**：课堂 RAG 示例面向问答，历史拼接、较高 temperature 和纯字符串解析不适合事实受控的问题卡片；项目需固定输入证据、固定字段并保留失败校验。
- **RAG 压缩包按原样运行**：包内 `embedding_models` 与 `src_data` 没有实际模型和业务文档，API key 也是空值；它提供的是流程骨架，不是可直接运行的 CampusParkGuard 知识库。
- **把检索链当成 Agent**：现有代码没有工具调用、参数校验、白名单和调用记录。它可支撑 RAG 与生成，但不能直接证明“受控 Agent”模块已经实现。
- **照搬 PyQt5 完整界面**：项目只需稳定演示上传、复核和结果展示；若主项目已有其他轻量界面方案，应只借鉴交互节奏，避免维护第二套 GUI。

## 4. 写入 project-design.md 的代码支撑说明

- YOLO MVP 可沿用课堂演示的 Ultralytics 图片推理流程，统一提取类别、置信度、边界框和标注图作为视觉证据。
- YOLO 微调采用课堂的数据准备、预训练权重训练、验证和 mAP/PR 指标流程，作为不阻塞 MVP 的最后独立实验。
- LLM 层可从 HuggingFace pipeline、Qwen 直接加载或 ModelScope 路线中择一封装，仅对既有事实和条款进行结构化表达。
- RAG 层复用 LangChain 的加载、切分、Embedding、Chroma、Retriever 与 LCEL 链路，并保留条款编号和来源用于引用。
- 课堂代码未提供区域规则和真正的工具调用 Agent；本项目需分别采用确定性区域规则与少量白名单工具封装，不能让 LLM 生成风险事实。
