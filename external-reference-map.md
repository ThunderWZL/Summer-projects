# 外部参考调研

> 调研边界：以下资料只用于校准 CampusParkGuard 的技术可行性、模块边界和演示方式。项目仍是一个月课程设计；外部资料不构成执法依据，也不用于扩展为实时监控或企业级管理平台。

## 1. 可直接支撑项目设计的参考

| 参考来源 | 关键信息 | 对 CampusParkGuard 的作用 | 应写入方案的位置 |
|---|---|---|---|
| [Ultralytics YOLO Predict 官方文档](https://docs.ultralytics.com/modes/predict/) | Predict 可直接接收单张图片、目录等输入；返回 `Results`，其中检测任务可读取 `boxes.xyxy`、`boxes.conf`、`boxes.cls`，并可用 `plot()`、`save()`保存标注结果；`conf` 可设置最低置信度。 | 证明“上传图片后快速得到类别、框、置信度和证据图”可用现成接口完成。MVP 只需静态图片推理，不必建设视频流平台。检测记录应保留模型名、类别、置信度、坐标和标注图路径。 | “YOLO 视觉检测”“MVP”“核心数据结构” |
| [Ultralytics YOLO Train 官方文档](https://docs.ultralytics.com/modes/train/) | Train 支持预训练权重、自定义数据集、Python/CLI 调用、训练参数配置和训练指标可视化；官方示例直接使用 `model.train(data=..., epochs=..., imgsz=...)`。 | 证明小规模校园场景数据可以在 MVP 完成后独立微调。方案应把数据收集、标注、训练/验证划分和基线—微调指标对比写成增强实验，而非系统可运行的前提。 | “YOLO 微调增强”“实施计划”“验收指标” |
| [COCO 数据集与类别配置](https://docs.ultralytics.com/datasets/detect/coco/)（[COCO 官网](https://cocodataset.org/)） | Ultralytics 维护的 COCO 配置明确列出 `person`、`bicycle`、`car`、`motorcycle`（类别 0～3）；COCO 预训练模型可输出这些常见对象的检测框。 | MVP 可先用 COCO 预训练模型识别人、汽车、自行车和摩托车。COCO 类别只能说明“看到了什么”，不能直接说明“违停/堵塞”；风险事实必须再由人工配置区域与确定性规则产生。 | “项目可行性”“YOLO 视觉检测”“设计原则” |
| [LangChain 官方 RAG 教程](https://docs.langchain.com/oss/python/langchain/rag) | 官方链路分为索引与运行时两段：加载文档、切分、Embedding、写入向量库；随后检索相关片段并把检索内容交给模型生成。教程同时给出简单检索工具和单次生成的两步式 RAG。 | 20～40 条管理条款足以采用轻量 RAG：离线构建索引，运行时按问题事实检索 Top-k 条款，返回正文、条款号、来源和链接。无相关片段时应返回“依据不足”，不能由 LLM 补造条款或风险事实。 | “RAG 条款检索”“总体架构”“数据结构” |
| [Dify Knowledge 官方文档](https://docs.dify.ai/en/cloud/use-dify/knowledge/readme) | Dify 将知识库描述为自有资料的 RAG 上下文，并提供文档/分块管理、检索测试和元数据过滤思路。 | 借鉴“可测试的知识库”和元数据管理：为条款保留 `rule_id`、主题、适用对象、区域类型、发布单位、原文链接和发布日期。Dify 不作为 MVP 的运行依赖。 | “条款库设计”“测试方案”“可扩展方向” |
| [Dify Workflow 官方快速入门](https://docs.dify.ai/en/quick-start) | 官方示例用输入、参数提取、IF/ELSE、LLM、模板和输出节点串联工作流，并强调结构化输出；规则型格式化交给模板而不是 LLM，可提高稳定性。 | 借鉴清晰的节点输入/输出、条件分支和结构化结果。项目中的事实判断继续由 Python 规则完成，问题卡片固定字段由模板/Schema 约束，Dify 仅可作为答辩中的工作流思想对照或可选演示。 | “Agent 编排”“问题卡片”“课程技术点对应关系” |
| [Dify Agent Strategy 官方文档](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/agent-strategy-plugin) | Function Calling Agent 由模型选择并调用已提供的工具，工具具有明确参数；示例还设置最大迭代次数。 | 支持采用“白名单工具 + 参数 Schema + 调用次数上限”的受控 Agent。只开放检测结果读取、规则事实读取、条款检索、卡片渲染和报告汇总等工具，不开放任意代码、网络搜索或自动处置。 | “Agent 编排”“安全边界” |
| [Qwen3 官方 Quickstart](https://github.com/QwenLM/Qwen3/blob/main/docs/source/getting_started/quickstart.md)、[阿里云 Qwen API 官方入门](https://help.aliyun.com/en/model-studio/first-api-call-to-qwen)、[Hugging Face Transformers Pipeline](https://huggingface.co/docs/transformers/main_classes/pipelines) | Qwen 官方给出 Transformers/ModelScope 本地推理和 OpenAI 兼容服务方式；阿里云文档提供 Qwen API 路径；Hugging Face `pipeline` 封装预处理、模型推理和后处理，可直接进行文本生成。 | 把 LLM 封装成统一 `generate()` 适配层：课程环境允许时使用轻量本地模型，资源不足时可切换 API。模型只负责依据已给事实与检索片段组织文字，任一调用方式都不能改变事实来源边界；API 也不应成为强依赖。 | “LLM 推理”“部署与降级策略”“课程技术点对应关系” |
| [上海交通大学机动车管理办法（试行）](https://bwc.sjtu.edu.cn/note/385)、[云南师范大学校园交通安全管理规定](https://bw.ynnu.edu.cn/info/1031/1881.htm) | 校园正式规则通常围绕按标志/车位停放、不得占用通道和出入口、不得占用消防通道、机动车与非机动车分区停放等可核验行为组织。云南师大规定还明确了车位线内停放、非机动车在指定车库（棚）或停车线内停放等规则。 | 为轻量条款库提供校园语境和可解释标签，例如“机动车进入禁停区”“车辆占用通行区”“非机动车停入楼梯出入口/人员通道”。正式演示应标注为“示例条款库”，并按目标学校届时有效规定复核。 | “知识库来源”“区域规则”“限制与声明” |
| [海南大学校园交通安全管理通知](https://security.hainanu.edu.cn/info/1023/2577.htm) | 通知列出人行道、绿化带、楼宇出入口、车辆与人员通道等禁停区域，并明确黄色网格线消防通道不得堵塞；也列出非机动车在人行道、斑马线和机动车位等位置的停放限制。 | 可把场景抽象为少量人工多边形类型：`parking_zone`、`no_parking_zone`、`fire_lane`、`entrance_exit`、`pedestrian_passage`。这样同一 YOLO 结果可用确定性空间关系匹配不同区域规则。 | “区域规则判断”“演示场景设计” |
| [应急管理部：关于规范电动车停放充电加强火灾防范的通告](https://www.mem.gov.cn/gk/tzgg/qt/201712/t20171231_397854.shtml)、[应急管理部对高层民用建筑消防安全管理规定的解读](https://www.mem.gov.cn/gk/zcjd/202107/t20210716_392214.shtml) | 官方资料明确公共门厅、疏散走道、楼梯间、安全出口等区域的电动自行车停放/充电风险，并倡导集中停放与充电。 | 条款库可收录消防与非机动车管理依据，但视觉模块只能报告画面可见且能由区域关系确定的“疑似占用/停放”事实，不能从单张图片推断充电状态、法律责任或处罚结论。 | “条款库边界”“风险事实边界”“免责声明” |
| [Ultralytics Parking Management 开源实现](https://docs.ultralytics.com/reference/solutions/parking_management) | 官方开源实现把停车区域坐标保存在 JSON 中，以检测框中心与区域多边形的空间关系计算占用，并输出标注图、占用数和空闲数。 | 直接支撑“区域配置与模型检测解耦”的轻量方案。CampusParkGuard 可借鉴 JSON 多边形和点在多边形内判断，但目标是生成疑似问题事实，不必扩展为完整停车位运营系统。 | “区域配置”“规则引擎”“核心数据结构” |
| [开源停车位 YOLO 项目](https://github.com/Arpitpatel1706/Car-Parking-Slot-Ocuupancy-detection-model)、[停车占用研究代码](https://github.com/Eighonet/parking-research) | 现有项目常按数据准备/标注、模型训练、区域或车位配置、推理展示分阶段组织，并用 precision、recall、mAP 等比较模型。 | 只借鉴“基线先跑通、训练单独实验、结果可视化”的项目组织方式；不照搬其实时视频、平台服务或专用车位分类路线。 | “实施顺序”“YOLO 微调增强”“答辩展示” |
| [2025 AI City Challenge 官方任务说明](https://www.aicitychallenge.org/2025-ai-city-challenge/) | 知名交通视觉赛事把对象检测、空间理解与交通安全描述作为可分别评价的任务；其任务也显示多摄像头、3D、视频问答等研究级方案成本很高。 | 借鉴“先形成可验证视觉证据，再生成结构化描述”的思想；课程项目只做单图检测、2D 区域规则和证据约束报告，不引入赛事级视频、多视角、3D 或开放式因果推理。 | “设计原则”“答辩亮点”“范围边界” |

条款库建议只从学校官网、保卫处和政府公开资料中人工摘录并复核。可按“机动车停放 8～10 条、非机动车停放 6～10 条、消防/疏散通道 6～10 条、校园通行区域 4～8 条”组织为约 24～38 条；每条只表达一个规则，并记录发布单位、适用范围、原文链接和核验日期。只有能映射到“可检测对象 + 已配置区域”的条款进入自动匹配，其余条款仅可被人工查询。

## 2. 对项目 idea 的成熟化建议

1. **以预训练模型先闭环。** MVP 固定处理上传的校园场景图片，优先筛选 COCO 已覆盖的 `person`、`bicycle`、`car`、`motorcycle`；输出类别、置信度、边界框和标注图。模型版本和置信度阈值写入运行记录，便于复现。
2. **把“检测”和“问题事实”分成两层。** YOLO 只产生对象事实；人工为每张示例图或固定机位配置少量多边形区域，规则引擎用类别、置信度和框中心/底边点与区域的关系生成 `suspected_issue`。LLM、RAG 都不得回写或改判这层事实。
3. **让规则少而可解释。** MVP 只实现 3～5 个规则模板，如“机动车位于禁停区”“车辆占用消防通道”“非机动车位于出入口/人员通道”“对象未落入允许停车区”。每次命中保留 `rule_id`、区域 ID、相关检测 ID 和计算理由，避免为追求数量堆叠脆弱规则。
4. **建立小而干净的条款库。** 人工整理 20～40 条，每条包含唯一编号、标题、单一规则正文、对象类型、区域类型、发布单位、来源 URL、适用范围与核验日期。RAG 仅返回 Top-k 依据及引用；没有匹配条款时明确标记“待人工核验”，不能生成处罚或法律结论。
5. **采用受控 Agent 而非自由自治。** 白名单仅包含 `read_detection`、`read_rule_fact`、`retrieve_policy`、`render_issue_card`、`summarize_report` 等只读或内容生成工具，并限制调用轮次。Agent 的输入必须包含已有问题事实，输出必须通过固定 Schema 校验。
6. **把问题卡片定位为证据载体。** 卡片展示原图/标注图、对象与置信度、命中区域、确定性规则说明、检索条款与来源、人工复核状态；状态只需“待复核/确认/排除”。它是报告输入，不实现派单、审批、通知或处置闭环。
7. **为 LLM 准备可降级路径。** 用统一适配层支持课堂已有的本地 Qwen/Hugging Face 调用或可选 API；固定低随机性、结构化输出和超时处理。LLM 不可用时仍能导出检测、规则事实和条款检索结果，保证核心链路可演示。
8. **最后再做 YOLO 微调。** 先冻结完整 MVP，再用小规模自建/公开数据完成标注、训练、验证和基线对比；比较 precision、recall、mAP 及代表性样例。时间不足时提交基线结果、数据样例和微调实验设计即可，不把高准确率或微调权重作为项目完成条件。

## 3. 不建议采用的外部复杂方案

- **实时多路视频、跨摄像头跟踪与 3D 空间理解。** 这类能力需要持续流处理、标定、跟踪数据和复杂评测，明显超过一个月课程设计；MVP 保持单图，视频抽帧只能作为后续扩展。
- **让多模态大模型直接看图判定违停或消防风险。** 输出难以复现且可能补造事实。视觉事实应来自 YOLO，问题事实应来自可审计区域规则；LLM 只组织既有证据。
- **大规模自建数据、从头训练或复杂模型结构改造。** 数据清洗、算力和调参风险过高。微调只做预训练小模型的小样本实验，并与 MVP 解耦。
- **把 Dify 私有化部署、插件开发或复杂工作流平台设为必选。** 部署与运维会挤占核心实现时间。只借鉴知识库、节点编排和工具调用思想；若课堂要求展示 Dify，可用同一份输入输出做可选流程原型。
- **开放式自主 Agent 或复杂多智能体协作。** 自主规划、网络搜索和任意工具调用会扩大不确定性，也没有必要。单个受控 Agent 加固定工具白名单足以体现课程技术。
- **企业级工单、权限、审批、消息通知和多部门流转。** 这些功能与算法课程目标关系弱，且会把项目误导为管理平台。问题卡片只做展示、复核和报告汇总。
- **人脸识别、人员身份追踪或强制车牌识别。** 它们带来隐私、合规、数据和模型成本，且不是当前闭环所需。若未来展示车牌扩展，只能脱敏并默认关闭；人脸识别不采用。
- **直接照搬竞赛级交通安全描述或开源实时停车系统。** 竞赛方案依赖视频、多视角和大数据，开源项目的环境与数据质量也未必适配校园。只吸收模块拆分、区域 JSON、证据可视化和指标对比方式，并以本项目数据单独验证。

> 限制说明：不同学校、校区和时期的管理规定可能不同；上述校规和政府资料只用于设计示例条款库及字段体系。正式课程演示应保留原文链接与核验日期，明确“疑似问题、待人工复核”，不得把系统输出表述为执法认定。外部开源仓库仅作为功能组织参考，不代表其模型、数据或工程质量已经过本项目验证。
