# 课程技术覆盖映射

本映射以一个月课程设计能完成、能演示、能解释为边界。CampusParkGuard 的唯一主线为：**YOLO 视觉检测 → 区域规则判断 → RAG 条款检索 → Agent 生成问题卡片和报告**。MVP 先打通闭环；YOLO 微调、第二种 LLM 推理后端和 Dify 可视化复刻作为增强，不建设企业级平台，也不追求所有课程技术的形式化堆叠。

## 1. 课程技术点覆盖表

| 课程技术点 | 所属课程日 | CampusParkGuard 中的体现方式 | MVP/增强/不采用 | 原因 |
|---|---|---|---|---|
| 深度学习基础 | 第 1 天 | 在设计与答辩中说明神经网络的前向传播、CNN 的局部连接/权重共享/分层特征，以及预训练模型到校园场景的迁移关系；实际运行中完成图像张量预处理和模型前向计算。 | MVP | 与视觉检测主线直接相关，解释 YOLO 的基础足够；无需从零实现神经网络。 |
| PyTorch 基础与推理流程 | 第 1 天 | 通过 YOLO 的 PyTorch 后端体现 Tensor、CPU/GPU device、权重加载、`eval()`、`no_grad()`、前向推理和结果转回可视化数据。 | MVP | 是可运行视觉模块的底层框架能力，工作量低且与课程内容一致。 |
| PyTorch 训练流程 | 第 1 天 | 在可选 YOLO 微调中体现 Dataset/DataLoader、数据增强、训练/验证划分、损失、反向传播、优化器、模型保存和训练曲线；不另行从零训练 CNN。 | 增强 | 完整训练需要标注集、算力和调参时间，不应阻塞主闭环。 |
| YOLO 目标检测 | 第 2 天 | 用预训练 YOLO 检测项目需要的少量目标类别，展示类别、边界框、置信度；文档中对应 one-stage、Backbone/Neck/Head、IoU 与 NMS。 | MVP | 是项目感知入口，也是课程目标检测技术最直接的应用。 |
| YOLO 推理 | 第 2 天 | 支持上传校园图片进行推理，输出叠加框图片和结构化检测结果；保留置信度阈值、NMS 后结果、耗时等基础信息供规则模块使用。 | MVP | 可快速形成稳定、直观且可验收的演示，不依赖自建大数据集。 |
| YOLO 微调 | 第 2 天 | 若通用权重对目标类别或校园视角效果不足，采集并标注小规模数据，基于预训练权重微调；用 Precision、Recall、mAP 和少量案例对比微调前后。 | 增强 | 能完整体现训练实践，但数据标注与调参不确定性高，只在 MVP 完成后开展。 |
| 区域规则判断 | 项目业务连接层（衔接第 2、4 天） | 在图片上配置少量多边形区域和规则，根据检测框中心点/重叠关系判断“目标是否进入限制区域”等事件，并输出规则 ID、证据框和判定结果。 | MVP | 把“检测到目标”变成明确的问题事件；规则应由代码确定，避免让大模型代替可验证的空间判断。 |
| 大语言模型推理 | 第 3 天 | 将检测事实、区域判定和检索条款组成受约束提示词，调用 LLM 生成结构化问题卡片与汇总报告；设置低随机性、固定字段并校验输出。 | MVP | 文本归纳与报告生成符合 LLM 能力；事实与判定仍来自前置模块，可减少幻觉。 |
| Qwen / API / 本地模型调用 | 第 3 天 | 建立统一模型适配接口，在 Transformers/ModelScope 本地小参数 Qwen 与 OpenAI-compatible API 中择一完成 MVP；模型名、服务地址和密钥均配置化，不要求同时维护两套后端。 | MVP | 一种可用后端已经足以支撑闭环，也能体现课程中的本地或 API 推理调用；择一可避免项目被设备或外部服务单点阻塞。 |
| 第二种推理后端与 llama.cpp | 第 3 天 | 在主后端稳定后，可增加另一种调用方式，或通过 llama.cpp 的 GGUF/Qwen 服务复用同一模型接口。 | 增强 | 可补充本地推理、量化模型和接口兼容实践，但模型下载、内存、环境与双后端测试会增加工作量。 |
| Prompt 与结构化输出 | 第 3、4 天 | 固定 system/user 模板，明确只允许引用输入事实和检索条款；问题卡片字段包含事件类型、位置、检测证据、匹配条款、处理建议和来源，失败时重试或返回模板结果。 | MVP | 比开放式生成更稳定，便于前端展示、测试和追溯。 |
| RAG | 第 4 天 | 将少量校园停车/通行/安全管理条款离线切分、嵌入并存入本地向量库；按事件描述检索 Top-k 片段，再连同来源元数据交给 LLM。 | MVP | 能补充项目专有知识、减少幻觉并提供条款依据；知识库规模保持课程设计级别。 |
| LangChain | 第 4 天 | 用 Loader、`RecursiveCharacterTextSplitter`、Embedding、Chroma/Retriever、Prompt 和 LCEL chain 实现“条款入库—检索—生成”；核心步骤保留独立函数，便于调试。 | MVP | 与课件及示例代码高度一致，能用较少代码清楚展示 RAG 组件链。 |
| Embedding 与向量检索 | 第 3、4 天 | 对条款和查询使用同一嵌入模型，持久化 Chroma；演示相似度检索结果与来源。MVP 不引入分布式向量数据库。 | MVP | 是 RAG 的关键技术点；课程设计数据量很小，本地向量库已经足够。 |
| Dify / 工作流 | 第 4 天 | 在代码版闭环稳定后，用 Dify 复刻一个最小工作流：开始 → HTTP/工具输入检测事实 → 知识检索 → LLM → 结束，并导出 DSL 或截图留作演示。 | 增强 | 可展示可视化编排，但与 LangChain 主实现功能重复；不应同时维护两套 MVP。 |
| Agent / 工具调用 | 第 4 天 | 构建受控的单 Agent，只暴露 `detect_scene`、`judge_zone_rules`、`retrieve_policy`、`generate_card_or_report` 四类白名单工具；编排器强制检测、规则、检索完成后才能生成，关键判定由工具返回而非自由猜测。 | MVP | 能体现“感知—规划—行动”和工具调用，同时用必经步骤、类型化输入输出和有限重试保持可解释、可测试。 |
| 复杂多 Agent、长期记忆、自我反思 | 第 4 天 | 不实现多 Agent 协作、开放式循环、自主联网和长期用户画像；只保存本次事件处理所需的中间结果。 | 不采用 | 对当前单一闭环没有必要，会显著增加状态管理、调试和验收难度。 |
| LLM 微调、MoE 训练、从零训练 CNN | 第 1、3 天 | 仅在原理说明中体现，不作为系统功能。 | 不采用 | 与项目目标不直接相关，所需数据、算力和时间远超一个月课程设计范围。 |

## 2. 当前项目应重点体现的技术

1. **端到端闭环**：用一张校园图片完整演示检测、规则判定、条款检索、问题卡片与报告生成，优先保证每一步都能看到输入和输出。
2. **YOLO 推理与结果解释**：保留类别、框坐标、置信度、阈值和推理耗时，能解释 IoU、NMS 以及检测结果如何进入下游。
3. **区域规则的确定性判断**：用少量多边形区域、框中心点或重叠比例形成可复现事件，不让 LLM 直接判断视觉违规。
4. **可追溯 RAG**：条款需要分段、嵌入、Top-k 检索并返回文档名/条款号等来源；生成内容必须绑定检索证据。
5. **LangChain 组件化实现**：清楚展示 Loader、Splitter、Embedding、Chroma、Retriever、Prompt、LLM 的数据流，而非只调用一个黑盒问答接口。
6. **受约束的大模型生成**：使用低随机性、字段化模板和输出校验，分别生成单事件问题卡片与多事件汇总报告。
7. **受控 Agent 与工具调用**：Agent 只编排四类项目工具，记录工具名、关键输入和结果；禁止开放式无限循环。
8. **基础效果评估**：至少准备若干正常/异常样例，统计检测与规则判断的成功情况，并人工核对条款相关性、引用正确性和报告字段完整性。
9. **统一模型调用接口**：根据设备和网络条件，在本地小参数 Qwen 与兼容 API 中择一完成可运行后端；将模型名、地址和密钥配置化，为后续切换后端留出单一适配点。

## 3. 当前项目只作为扩展体现的技术

- **YOLO 微调**：在 MVP 的预训练推理确有类别或场景短板、且已有可用标注数据时再做。微调应控制类别和数据规模，只比较关键指标与代表性样例，不开展大规模模型搜索。
- **完整 PyTorch 训练实验**：跟随 YOLO 微调体现数据、增强、训练、验证和保存即可；不另起一个从零 CNN 项目，避免脱离主线。
- **第二种 LLM 推理后端**：MVP 只需在本地小参数 Qwen 与兼容 API 中选定一种；另一种后端以及 llama.cpp/GGUF 量化部署留作增强，避免双套环境和双套测试拖慢主线。
- **Dify 可视化工作流**：只复刻已验证的核心流程并导出 DSL/截图，用于说明节点、变量、知识检索和 LLM 编排；代码版 LangChain 仍是唯一主实现。
- **混合检索、Rerank、语义分块**：只有基础向量检索出现明显条款错配时才逐项尝试。小型规则库优先调好分段、元数据和 Top-k，不引入复杂检索基础设施。
- **ONNX 或边缘部署**：如时间充足，可导出视觉模型或展示模型结构；项目验收不要求跨平台、移动端或嵌入式部署。
- **复杂 Agent 能力**：多 Agent、长期记忆、自我反思、联网搜索和动态规划不进入 MVP；当前任务用受控单 Agent 足以覆盖工具调用课程点。

## 4. 建议写进 project-design.md 的“课程技术点对应关系”

CampusParkGuard 以课程四天技术形成单一闭环：第 1 天的深度学习、CNN 与 PyTorch 支撑图像张量处理和模型推理；第 2 天使用 YOLO 完成目标检测、边界框与 NMS 后处理，并由确定性区域规则生成事件；第 3 天通过统一接口调用本地 Qwen 或兼容 API，按结构化提示生成问题卡片和报告；第 4 天使用 LangChain 完成条款切分、嵌入、Chroma 检索和可追溯 RAG，再由受控 Agent 调用白名单工具。MVP 先完成预训练模型闭环，YOLO 微调、第二推理后端与 Dify 工作流复刻作为增强项。
